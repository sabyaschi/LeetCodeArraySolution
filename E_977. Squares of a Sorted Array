class Solution {
    public int[] sortedSquares(int[] nums) {
        int ans[] = new int[nums.length];
        int j = 0;
        for( int i : nums){
            int tmp = i * i;
            ans[j] = tmp;
            j++;
        }

        Arrays.sort(ans);
        return ans;
    }
}
