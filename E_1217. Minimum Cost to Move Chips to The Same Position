class Solution {
    public int minCostToMoveChips(int[] position) {
        int o = 0 , e = 0;
        for(int i : position){
            if(i % 2 == 1){
                ++o;
            }
            else{
                ++e;
            }
        }
        return Math.min(o, e);
    }
}
